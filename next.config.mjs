/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, // Oublie pas de le enable quand tu build (reason : helps with identifying potential issues)
  };
  
  export default nextConfig;
  